# RC35 — Promo number input style fix

- Removed the browser-native white increment/decrement controls from numeric fields in the promo-code form.
- Numeric values can still be entered manually and changed with the keyboard arrow keys.
- No Supabase SQL or Edge Function redeploy is required.
