# RC34 — Promo form stability fix

- Виправлено стрибок модального вікна при перемиканні «Без обмеження».
- Виправлено такий самий стрибок для «Без дати завершення».
- Позиція внутрішнього скролу та сторінки фіксується до взаємодії й відновлюється після зміни стану.
- Поля ліміту та дати не видаляються з DOM: вони лише переходять у disabled-стан.
- При відкритті форма завжди починається зверху; фокус на коді не прокручує модалку.
- Додано захист від scroll anchoring і стабільну ширину області прокрутки.

Supabase, SQL і Edge Functions повторно розгортати не потрібно.
