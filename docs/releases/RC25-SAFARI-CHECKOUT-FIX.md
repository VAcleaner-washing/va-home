# RC25 — Safari / PWA Nova Poshta fix

- The city and warehouse results panel follows `visualViewport` and stays above the iPhone keyboard.
- A saved city name without Nova Poshta references no longer disables autocomplete.
- Existing RC22/RC23 Supabase functions are still required. This RC25 change itself does not require another Edge Function deploy.
