# VA HOME v11.8.35

## Виконано

1. Виправлено регістр назв WebP-файлів у `images/product-story/` для:
   - `silent-temple`
   - `dark-bloom`
   - `moss-and-shadow`
   - `the-archive`

   Усі назви тепер у lowercase та відповідають шляхам, які формують `js/product.js` і `js/gallery-engine.js`.

2. Без зміни HTML і шляхів оптимізовано 12 найважчих WebP-файлів.
   - збережено формат WebP;
   - великі джерела зменшено максимум до 1600–1920 px по ширині;
   - загальна економія цих 12 файлів: приблизно 1.76 MB;
   - усі 147 WebP успішно проходять перевірку декодування.

## Свідомо не виконано

- Не змінювалася верхня галерея товарних сторінок.
- Не створювалися штучні варіації трьох візуально повторюваних фотографій. Для них потрібні три нові унікальні сцени.
- Не додано масово AVIF/srcset: це потребує окремого проходу по HTML, CSS-фонах і динамічно створюваних зображеннях та має тестуватися як окрема оптимізація.
- CSS-файли не об'єднувалися, щоб не змішувати безпечні виправлення з ризиковим рефакторингом каскаду.
